// eslint-disable-next-line no-shadow
export enum Module {
  INHOUSE,
  OUTHOUSE,
  HOST,
  PROCESSORGENERAL,
  CRM,
  DEPOSITS,
  GIFTSRECEIPTS,
}

// eslint-disable-next-line no-shadow
export enum MovementType {
  CONTACT,
  BOOKING,
  SHOW,
  SALE,
  FOREIGNINVIT,
}

// eslint-disable-next-line no-shadow
export enum Program {
  ALL,
  INHOUSE,
  OUTHOUSE,
}

// eslint-disable-next-line no-shadow
export enum InvitationMode {
  READONLY,
  ADD,
  EDIT,
  EDITPARTIAL,
  SEARCH,
  DELETE,
}
